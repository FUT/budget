!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'Budget'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Budget'}"}
    = stylesheet_link_tag "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    -# application layout styled for Twitter Bootstrap 2.3
    .container
      .header
        %ul.nav.nav-pills.pull-right
          %li.active
            %a{href: '#'} Home
          %li
            %a{href: '#'} About
          %li
            %a{href: '#'} Contact
        %h3.text-muted My Budget
      .main
        .span12
          = render 'layouts/messages'
          = yield
      .footer
